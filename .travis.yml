cache: bundler

language: ruby

rvm:
  - 2.2.3

addons:
  postgresql: '9.4'

before_script:
  - psql -c "create database openipsumapitest;" -U postgres

notifications:
  email: false

script:
  - bundle exec rake rubocop
  - bundle exec rake spec
